<tr>
  <td><%= purchase.try(:purchaser).try(:name) %></td>
  <td><%= purchase.try(:item).try(:description) %></td>
  <td><%= number_to_currency purchase.try(:item).try(:price) %></td>
  <td><%= purchase.quantity %></td>
  <td><%= number_to_currency purchase.gross_value %></td>
  <td><%= purchase.try(:merchant).try(:name) %></td>
  <td><%= purchase.try(:merchant).try(:address) %></td>
</tr>
